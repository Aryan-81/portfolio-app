/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
  // assetPrefix: './', 
  // basePath: '/portfolio-app',  
};

export default nextConfig;
